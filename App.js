// @flow
import React from 'react';
// import Config from 'react-native-config';
import PrimaryStackNavigationComponent from './app/navigations/primaryStackNavigator';

export default class app extends React.PureComponent<any, any> {
  render() {
    return (
      <PrimaryStackNavigationComponent />
    );
  }
}
